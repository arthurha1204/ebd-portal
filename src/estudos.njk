---
layout: base.njk
title: Estudos e Aulas
---

<main style="padding-top: 3rem;">
    <div class="container">
        <h1 style="text-align: center; margin-bottom: 2rem;">Nossos Estudos</h1>

        <h2 style="border-bottom: 1px solid #e2e8f0; padding-bottom: 0.5rem; margin-bottom: 1.5rem;">Séries de Estudo</h2>
        <div class="series-grid" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 2rem; margin-bottom: 4rem;">
            {% for serie in collections.series %}
            <div class="series-card" style="border: 1px solid #e2e8f0; border-radius: 6px; overflow: hidden;">
                <div style="background: {{ serie.data.color }}; padding: 2rem; text-align: center; color: #fff;">
                    <div style="font-size: 2rem; font-weight: 800; opacity: 0.3;">{{ serie.data.sigla }}</div>
                    <h3 style="color: #fff; margin-top: -10px;">{{ serie.data.title }}</h3>
                </div>
                <div style="padding: 1rem; text-align: center;">
                    <a href="{{ serie.url }}" class="btn btn-outline" style="width: 100%;">VER SÉRIE</a>
                </div>
            </div>
            {% endfor %}
        </div>

        <h2 style="border-bottom: 1px solid #e2e8f0; padding-bottom: 0.5rem; margin-bottom: 1.5rem;">Materiais de Aula & Artigos</h2>
        <div class="feed-grid">
            {% for post in collections.all | reverse %}
                {% if ('/aulas/' in post.url or '/artigos/' in post.url) and post.data.title %}
                <article class="card">
                    <div style="height: 180px; position: relative; overflow:hidden;">
                        <span class="content-type-badge" style="background-color: {% if '/aulas/' in post.url %}var(--accent-color){% else %}var(--primary-color){% endif %};">
                            {% if '/aulas/' in post.url %}AULA{% else %}ARTIGO{% endif %}
                        </span>
                        <img src="{{ post.data.image or '/assets/fundo.jpg' }}" style="width:100%; height:100%; object-fit:cover;">
                    </div>
                    <div class="card-content">
                        <h3 style="font-size: 1.1rem;">{{ post.data.title }}</h3>
                        <p style="font-size: 0.85rem; color: #64748B;">{{ post.date | postDate }}</p>
                        <a href="{{ post.url }}" class="btn btn-outline" style="width: 100%; margin-top: auto;">LER</a>
                    </div>
                </article>
                {% endif %}
            {% endfor %}
        </div>
    </div>
</main>